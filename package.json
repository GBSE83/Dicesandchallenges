{
  "name": "dice-game",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build required' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "peer": "^0.6.1"
  }
}